# Используем официальный образ PHP
FROM php:8.2-cli

# Устанавливаем рабочую директорию
WORKDIR /app

# Копируем файлы проекта в контейнер
COPY . /app

# Команда по умолчанию при запуске контейнера
CMD [ "php", "src/PalindromeChecker.php" ]